# Hatch Review soft patch — starters-v1

Applied 2026-09-24 13:01 IST. Non-blocking asset-only patch; the style lock was not reopened or edited.

## Review soft items closed

- **Sparkpup flame @32:** patched `static/sparkpup_32.png`, every `idle/sparkpup_idle_*_32.png`, and every `merge/sparkpup_merge_*_32.png`. The top mark now has a slightly broader, more saturated coral-peach `#F4A88A` outer ember mass, with the cream core reduced enough to read as flame/ember. Two black face dots, flat `#F7C4A8` body, and `#2C2A28` outline are retained.
- **Sparkpup @64 consistency:** applied the same composite-path adjustment to `static/sparkpup_64.png`, all `idle/sparkpup_idle_*_64.png`, and all `merge/sparkpup_merge_*_64.png`.
- **Cottonwisp outline:** patched `static/cottonwisp_{32,64}.png`, every `idle/cottonwisp_idle_*_{32,64}.png`, and every `merge/cottonwisp_merge_*_{32,64}.png`. The existing cloud-body silhouette is retained, its charcoal outline is expanded to the requested ~2px minimum at 32 and proportionate 64px weight, and the body is normalized to flat `#C5D9F0`. No second mark halo was added.

## QC refreshed

- `qc/qc_3x3_contact.png`
- `qc/preview_static_64.png`

No Sproutling files were changed.
